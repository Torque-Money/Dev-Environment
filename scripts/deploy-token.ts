import hre from "hardhat";
import fs from "fs";
import getFutureAddress from "../utils/getFutureAddress";

async function main() {}

main()
    .then(() => process.exit(0))
    .catch((error) => {
        console.error(error);
        process.exit(1);
    });
